Store binary AND of A, X and 3rd Instruction Byte (undocumented instruction)
M $\leftarrow$ A $AND$ X $AND$ B3

NOTE: This instruction is unstable on many 6502 processors, and should be avoided.

This instruction stores the binary AND of the contents of the Accumulator Register, X Register
and the third byte of the instruction
into the indicated location.

