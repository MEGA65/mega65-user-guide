Branch on Carry Flag Clear
6502:C$=$0 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8
4510:C$=$0 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8 $or$ PC $\leftarrow$ PC $+$ R16

This instruction branches to the indicated address if the
Carry Flag is clear.

