Branch on Carry Flag Set
6502:C$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8
4510:C$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8 $or$ PC $\leftarrow$ PC $+$ R16

This instruction branches to the indicated address if the
Carry Flag is set.

