Branch on Zero Flag Set
6502:Z$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8
4510:Z$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8 $or$ PC $\leftarrow$ PC $+$ R16

This instruction branches to the indicated address if the
Zero Flag is set. BEW stands for branch if equal, because
a CMP will result in the zero flag being set if the operants
are equal.

