Branch on Negative Flag Set
6502:N$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8
4510:N$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8 $or$ PC $\leftarrow$ PC $+$ R16

This instruction branches to the indicated address if the
Negative Flag is set. BMI stands for branch on minus.

