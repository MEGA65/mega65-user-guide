Branch on Zero Flag Clear
6502:Z$=$0 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8
4510:Z$=$0 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8 $or$ PC $\leftarrow$ PC $+$ R16

This instruction branches to the indicated address if the
Zero Flag is clear. BNE stands for Branch if not equal, because
a CMP will result in the zero flag being cleared if the operants
are not equal.

