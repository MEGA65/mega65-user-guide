Branch on Negative Flag Clear
6502:N$=$0 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8
4510:N$=$0 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8 $or$ PC $\leftarrow$ PC $+$ R16

This instruction branches to the indicated address if the
Negative Flag is clear. BPL stands for branch on plus.

