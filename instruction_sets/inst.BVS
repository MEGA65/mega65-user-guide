Branch on Overflow Flag Set
6502:V$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8
4510:V$=$1 $\Longrightarrow$ PC $\leftarrow$ PC $+$ R8 $or$ PC $\leftarrow$ PC $+$ R16

This instruction branches to the indicated address if the
Overflow (V) Flag is set.

