Clear Decimal Flag
D $\leftarrow$ 0

This instruction clears the Decimal Flag.
Arithmetic operations will use normal binary arithmetic, instead of
Binary-Coded Decimal (BCD).

\subsubsection*{Side effects}
\begin{itemize}
\item The D flag is cleared.
\end{itemize}