Return From Subroutine
PC $\leftarrow$ STACK $or$ PC $\leftarrow$ STACK + M, SP $\leftarrow$ SP $-$ 2

This instruction adds an optional argument to the Stack Pointer (SP) Register, and then
pops the Program Counter (PC) register from the stack, allowing
a routine to return to its caller.

