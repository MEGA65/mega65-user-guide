Store Binary AND of Accumulator and X Register
M $\leftarrow$ A $AND$ X

This instruction stores the binary and of the contents of the X Register
and the Accumulator Register
into the indicated location.

