base-page quad,X
4
8
$^{a}$
BP:MEM(<(B1)) + X
Base-Page Quad,X
This addressing mode operates on the Xth byte following the indicated base-page address.

For example, if X contained \$04, LDQ \$12,X would load the Q pseudo register from
addresses \$16(LSB) -- \$19(MSB) in the base-page.
