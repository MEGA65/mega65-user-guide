base-page quad,Y
4
8
$^{b}$
BP:MEM(<(B1)) + Y
Base-Page,Y
This addressing mode operates on the Yth byte following the indicated base-page address.

For example, if Y contained \$04, LDQ \$12,Y would load the Q pseudo register from
addresses \$16 -- \$19 in the base-page.
