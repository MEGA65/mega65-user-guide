immediate 8bit
imm
2
2

B1
Immediate
This addressing mode uses the supplied argument directly,
rather than using it as an address.

For example, LDA \#\$12 would load the accumulator with
the value \$12, regardless of the contents of memory location
\$12.
