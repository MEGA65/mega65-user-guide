base-page,X
bpX
2
3
$^{a}$
BP:MEM(<(B1)) + X
Base-Page,X
This addressing mode operates on the Xth byte following the indicated base-page address.

For example, if X contained \$04, LDA \$12,X would load the accumulator from
address \$16 in the base-page.
