base-page,Y
bpY
2
3
$^{b}$
BP:MEM(<(B1)) + Y
Base-Page,Y
This addressing mode operates on the Yth byte following the indicated base-page address.

For example, if Y contained \$04, LDA \$12,Y would load the accumulator from
address \$16 in the base-page.
